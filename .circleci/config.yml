version: 2
jobs:
  run_test:
    docker: 
     - image: dannydainton/htmlextra
    steps:
      - checkout
      - run: newman run tests/Restful_Booker_Collection.postman_collection.json -e ./tests/Restful_Booker_Environment.postman_environment.json -r htmlextra
      - store_artifacts:
          path: ~/test-results/newman
workflows:
    version: 2
    main:
        jobs:
            - run_tests
